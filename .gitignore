# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# 오프라인 설치 번들(scripts/build_offline_bundle.py 산출물) -- wheel/가중치
# 바이너리라 git에 안 올린다. 대상 머신으로는 이 디렉터리를 직접 전송한다.
offline_bundle/

# Virtual environments
.venv

# 로컬 자격증명(커밋 금지)
.env
.env.*
!.env.example
