"""Local-test FastAPI app. Not part of the library contract.

Requires the 'api' extra. This package deliberately consumes only the public
API of document_parser, so it doubles as a rehearsal of the integration
experience backend consumers will have.
"""
